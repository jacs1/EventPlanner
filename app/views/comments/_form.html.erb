

<%= form_for :comment, event_comments_path, method: :post do |f| %>

  <div class="field">
    <%= f.text_area :body, rows: 8 %>
  </div>
  <div class="actions">
    <%= f.submit %>

  </div>
<% end %>